import React from "react";

const CurrentContest = (props) => {
    return (
        <React.Fragment>
           <h2>This is the current contest</h2>
           <p>Contest X</p>
        </React.Fragment>
    );
};

export default CurrentContest;